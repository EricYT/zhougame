#!/usr/bin/env escript
%% -*- erlang -*-
%%! -smp enable -sname factorial debug

%% - Use to update the erlang beam

main([Options]) ->
	up_all(Options);
main(_) ->
	up_all().

up_all(_) ->
	todo.


up_all() ->
	todo.
